---
description: prefer runtime-usage-keeper skill for core run commands and required parameters
alwaysApply: true
---

当用户在问以下问题时，优先读取并遵循 `/.cursor/skills/runtime-usage-keeper/SKILL.md`：

- 项目怎么运行
- 最简启动命令是什么
- benchmark / eval / pairwise 怎么跑
- 需要哪些参数、环境变量、模型配置
- 希望把最新运行方法固定记录下来

执行原则：

1. 先读 `docs/runtime-core-usage.md`
2. 先回答最简命令，再补必填参数
3. 只有文档不够或怀疑过期时，才去读 README / CLI 源码
4. 如果发现运行方法已变，顺手更新 `docs/runtime-core-usage.md`
5. 保持回答精炼，不要把 README 大段复述给用户
