---
alwaysApply: true
---
## Commit Message 格式

```
<type>: <简短描述>
```

**类型**：`feat` | `fix` | `docs` | `refactor` | `chore` | `ci` | `test`

**要求**：
- 标题 ≤ 72 字符
- 动词开头，祈使语气
- 中文优先
- 不以句号结尾

---

## 示例

✅ `feat: 添加用户认证功能`
✅ `fix: 修复场景加载失败问题`
✅ `chore: 更新依赖版本`

❌ `feat: 完成了XXX功能，修复了YYY问题`

---

## Commit 执行规则

**重要**：除非用户明确授权，否则不要自动执行 commit。

- 每次 commit 前必须先展示 diff 和建议的 commit message
- 只有用户明确确认后才执行 commit
- 如果用户只说"帮我提交"或"commit"，则执行；如果只是讨论改动，则不执行
