---
description: 代码解读与分析规范。当需要阅读、理解、解释代码结构、模块职责、调用链或执行流程时激活。适用于代码分析、重构方案设计、模块间依赖梳理等场景。
globs:
  - "**/*.py"
  - "**/*.mdc"
  - "**/*.json"
  - "**/*.yaml"
  - "**/*.toml"
alwaysApply: false
---

# 代码解读规范

## 核心原则

- **层次递进**：先整体后细节，先结构后逻辑
- **按需详略**：核心代码逐行，辅助代码概述
- **精炼表达**：无废话，直接呈现

## 解读顺序

```
1. 数据结构（dataclass/enum）
2. 配置常量（prompts/config）
3. 基础设施（_llm_client/_logging）
4. 核心逻辑（agent）
5. 入口组装（main）
```

## 格式规范

### 代码引用

```startLine:endLine:filepath
code
```

### 新代码示例

```python
code
```

### 表格

| 列1 | 列2 |
|-----|-----|
| val | val |

## 解读结构

```
# 模块名

## 概览
职责一句话 + 架构图（调用链）

## 数据结构
关键类/函数定义

## 核心逻辑
重点函数逐行解读

## 执行示例
输入→输出

## 设计要点
关键决策
```

## 要求

| 必须 | 避免 |
|------|------|
| 架构图/调用链 | 逐行解释每行代码 |
| 完整执行示例 | 注释式废话 |
| 设计要点 | 冗余总结 |
| 输入/输出/作用 | 无示例 |

## 语言

**始终使用中文**
